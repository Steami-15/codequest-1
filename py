import sys
import math
import string
cases = int(sys.stdin.readline().rstrip())
for caseNum in range(cases):

    
    line = input().rstrip()
    
    line = line.split(" ")
    
    radius = int(line[0])
    
    width = int(line[1])
    
    height = int(line[2])
